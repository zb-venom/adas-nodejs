{{> head }}
<body>
    {{#if status.online}}
        {{> navbar}}
        {{#if status.none}}        
        <div class="content">
            {{> none}}
        </div>
        {{else}}
        <div class="content">
            {{{ body }}}
        </div>
        {{/if}}
    {{else}}
        {{{ body }}}
    {{/if}}
    <div class="made-by">made by Venom</div>
</body>
</html>