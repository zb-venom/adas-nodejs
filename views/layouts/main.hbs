{{> head }}
<body>
    {{#if status.online}}
        {{> navbar}}
        {{#if status.none}}        
        <div class="content">
            {{> none}}
        </div>
        {{else}}
        <div class="content">
            {{{ body }}}
        </div>
        {{/if}}
    {{else}}
        {{{ body }}}
    {{/if}}
    <a href="https://github.com/zb-venom/" target="_blank" class="made-by">made by Venom</a>
</body>
</html>